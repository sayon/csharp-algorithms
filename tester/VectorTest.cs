using System;
using Microsoft.VisualStudio.TestTools.UnitTesting;
using Algorithmis;

namespace tester
{
    [TestClass]
    public class VectorTest
    {
        [TestMethod]
        public void TestPush()
        {
           /* Vector<int> v = new Vector<int>();
            v.Push(4);
            v.Push(5, 4, 3);
            Assert.AreEqual(v.Pop(), 3);
            Assert.AreEqual(v.Pop(), 4);
            Assert.AreEqual(v.Pop(), 5);
            Assert.AreEqual(v.Pop(), 4);*/
        }
    }
}
